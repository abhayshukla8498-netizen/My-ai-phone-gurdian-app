name: Build APK from Zip

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # Yeh step spaces aur brackets wali zip file ko perfectly handle karega
      - name: Unzip Project Perfectly
        run: |
          mkdir -p extracted_project
          unzip -o "*.zip" -d extracted_project
          echo "Unzip completed successfully."

      - name: Grant execute permission for gradlew
        run: |
          cd extracted_project
          chmod +x gradlew
        continue-on-error: true

      # Yeh step direct extracted folder ke andar jaakar APK build karega
      - name: Build APK with Gradle
        run: |
          cd extracted_project
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Abhay-AI-Debug-APK
          path: 'extracted_project/**/build/outputs/apk/debug/*.apk'
          
